---
title: "U-Boot - Universal Bootloader"
date: 2026-05-19 09:20:00 +0700
categories: [Kernel]
tags: [Linux]
---

# 🚀 Tổng quan về U-Boot
U-Boot (Universal Bootloader) là bootloader mã nguồn mở phổ biến nhất trong thế giới Embedded Linux.

## Vai trò
Khi board nhúng được cấp nguồn, CPU không thể tự boot Linux vì:

* DRAM chưa được khởi tạo — cần cấu hình timing mới dùng được
* Kernel nằm trên Flash/eMMC — CPU không thể thực thi trực tiếp từ đó
* Kernel cần biết địa chỉ RAM, DTB — chưa có ai cung cấp thông tin đó.


Cần chương trình linh hoạt hơn cho việc boot linh hoạt thì dùng bootloader chạy lựa chọn chương trình để boot, ví dụ như **U-Boot**.

## Stage
### First Stage Bootloader (FSBL)
Thường gọi là boot ROM. ROM code trong các thiết bị chạy linux thường là chương trình vdk rất bé (1-2KB) lưu luôn trong chip. Thường lưu trong SOC.


Chương trình sẽ được lưu trong address ví dụ beagle bone black là0x2000 trong SOC. Khi bật nguồn chương trình bắt đầu nạp boot rom vào CPU để xử lý.


**Nhiệm của FSBL:**
- Khởi tạo 1 số clock.
- Pin muxing.
- Disable watchdog...
- Đọc giá trị của **SYSBOOT_PIN** để load second stage bootloader (SSBL) từ thiết bị nào emmc/NOR-flash/SD/USB...
#### Secure boot
Chương trình boot rom của chip AM33xx có hỗ trợ tính năng secure boot bằng hardware.
- ROM code lưu public key và private key.
- Image lưu trên thiết bị nhớ phải được mã hóa bằng public key.
- Sau  khi load image và giải mã trên ram, chương trình ROM sẽ check tính hợp lệ của image.
  
### Second Stage Bootloader (SSBL)
Do đặc thù kích thước nhỏ và không thể ghi đè lại. Nên chương trình boot ROM thường sẽ làm rất đơn giản. Tuy nhiên ngày nay, có nhu cầu về những cách thức boot phức tạp hơn. Ví dụ như cho phép boot OS từ ethernet, serial, hiển thị logo, boot song song nhiều OS, ...
- Do đó cần phải có 1 chương trình bootloader phức tạp hơn. Chương trình sẽ được lưu ở bộ nhớ ngoài với kích thước lớn hơn. Uboot ra đời.


Uboot đống vai trò là second stage (SPL) và third stage (TPL) boot loader. Những tình huống trong dự án mà chúng ta phải làm việc với uboot:
- Check phần cứng. Một số lỗi xảy ra mà không thể phân biệt được đó là lỗi của hardware/software.
- Cấu hình lại chân pin. Giả sử board BBB đang có mặc định 4 cổng serial. Chúng ta cần muốn lấy chân pin của 3 cổng để dùng vào việc khác => Cấu hình lại chân pin.
- Sửa lại giao diện boot (ví dụ có logo khi bật nguồn, ...)
- Nhìn chung, đa số công việc mà chúng ta làm với uboot sẽ làm nhiệm vụ chuẩn bị môi trường để boot được linux từ thiết bị lưu trữ.


1 code uboot được sinh ra 1 file là second stage là MLO và 1 file là third stage là file uboot. Cả 2 đều được lưu vào trong vùng nhớ đầu tiên của thẻ.
### Third Stage Bootloader (TSBL)

## 4. Flow boot (Linux Kernel Startup)
Quá trình khởi động Kernel diễn ra tuần tự:
1. **Khởi tạo kiến trúc CPU** (phân tích DTB, setup memory map).
2. **Khởi tạo Virtual Memory** (bật MMU, page table).
3. **Khởi tạo bộ lập lịch** (run queue, CFS).
4. **Khởi tạo File System** (VFS, mount `/proc`, `/sys`).
5. **Khởi tạo Device Drivers** (platform, bus drivers).
6. **Khởi tạo init process** (mount rootfs thật, chạy `/sbin/init` với PID 1).
## Test board vừa porting xong
https://hub.digi.com/dp/path=/support/asset/u-boot-reference-manual/


