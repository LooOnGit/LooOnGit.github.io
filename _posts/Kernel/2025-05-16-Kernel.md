---
title: "Kernel"
date: 2025-05-16 09:37:00 +0700
categories: [Kernel]
tags: [Kernel]
---

# 🐧 Linux Kernel

## 📌 Giới thiệu
Linux Kernel là nhân của hệ điều hành Linux, đóng vai trò quan trọng trong việc quản lý tài nguyên hệ thống và cung cấp interface giữa phần cứng và phần mềm.

![alt text](/assets/Linux/Kernel/kernel.png)


## 🧩 1. Native Compile (biên dịch nội bộ) và Cross Compile
### Cross compile
Cross compile nghĩa là biên dịch mã nguồn trên một hệ thống (host) để tạo ra chương trình chạy trên một hệ thống khác (target).

**Ví dụ:**
- Bạn viết code trên máy tính x86 (Windows hoặc Linux),
- Nhưng bạn muốn chương trình chạy trên bo mạch ARM (như STM32, ESP32, Raspberry Pi, hoặc Renesas RZ).

### Native Compile
Là quá trình biên dịch và chạy chương trình trên cùng một hệ thống (host = target).

## 📘 2. Kernel Basics

### 2.1 🌳 Cấu trúc Kernel
1. **Monolithic Kernel**
   - Process Management
   - Memory Management
   - File System
   - Device Drivers
   - Network Stack

2. **Kernel Subsystems**
   - VFS (Virtual File System)
   - Network Stack
   - Block I/O
   - Character Devices
   - IPC (Inter-Process Communication)

### 2.2 🔄 Process Management
1. **Process Scheduling**
   - CFS (Completely Fair Scheduler)
   - Real-time schedulers
   - Process priorities

2. **Process Creation**
   - fork() và exec()
   - Copy-on-write
   - Process context

### 2.3 💾 Memory Management
1. **Virtual Memory**
   - Page Tables
   - TLB Management
   - Memory Mapping

2. **Memory Allocators**
   - Buddy Allocator
   - Slab Allocator
   - vmalloc

## 🛠 3. Kernel Development

### 3.1 🔧 Kernel Module Programming
```c
#include <linux/module.h>
#include <linux/kernel.h>
#include <linux/init.h>

static int __init hello_init(void)
{
    printk(KERN_INFO "Hello, Kernel!\n");
    return 0;
}

static void __exit hello_exit(void)
{
    printk(KERN_INFO "Goodbye, Kernel!\n");
}

module_init(hello_init);
module_exit(hello_exit);
```
Trong đó:
- `__init` là macro của kernel header để cho kernel biết khi khởi tạo thì hàm này load 1 lần.
- `__exit` là macro của kernel header để cho kernel biết khi unload thì hàm này unload 1 lần.
- `module_init()` và `module_exit()` là macro của kernel header để khai báo cho kernel biết khi load và unload thì hàm này load và unload 1 lần.


### 3.2 📱 Device Driver Development
1. **Character Drivers**
   - File Operations
   - IOCTL Interface
   - User Space Interface

2. **Block Drivers**
   - Request Queue
   - Bio Layer
   - Block Layer

3. **Platform Drivers**
   - Device Tree
   - Platform Data
   - Resource Management

### 3.3 🐞 Setup Evironment
1. **Linux kernel header**
    - Là thành pần đuọc sử dụng để biên dịch cho module của kernel.
    - Kernel header được cài đặt phải trùng với kernel version mà bạn muốn sử dụng (uname -r).
    - **Cú pháp:**
```bash
    sudo apt install -y linux-headers-`uname-r`
```
    - Đường dẫn: `/lib/modules/`

![alt text](/assets/Linux/Kernel/header_1.png)

Trình tự cài đặt kernel header:
```bash
uname -r
sudo apt update
sudo apt install linux-headers-$(uname -r)
```

2. **Init/Exit Funtion**
   - **__init** là chức năng đầu tiên được thực thi khi module được load vào kernel.
   - **__exit** là chức năngđược thực thi sau cùng khi module được unload khỏi kernel.

```c
/**
 * @file    hello.c
 * @author  Akshat Sinha
 * @date    10 Sept 2016
 * @version 0.1
 * @brief  An introductory "Hello World!" loadable kernel
 *  module (LKM) that can display a message in the /var/log/kern.log
 *  file when the module is loaded and removed. The module can accept
 *  an argument when it is loaded -- the name, which appears in the
 *  kernel log files.
*/
#include <linux/module.h>     /* Needed by all modules */
#include <linux/kernel.h>     /* Needed for KERN_INFO */
#include <linux/init.h>       /* Needed for the macros */

#define DRIVER_AUTHOR "Loo"
#define DRIVER_DESC   "A simple Hello world LKM!"

/*Constructor*/
static int __init hello_start(void)
{
    printk(KERN_INFO "Loading hello module...\n");
    printk(KERN_INFO "Hello world\n");
    return 0;
}

/*Destructor*/
static void __exit hello_end(void)
{
    printk(KERN_INFO "Goodbye Mr.\n");
}

module_init(hello_start);
module_exit(hello_end);

MODULE_LICENSE("GPL");
MODULE_AUTHOR(DRIVER_AUTHOR);
MODULE_DESCRIPTION(DRIVER_DESC);

MODULE_VERSION("0.1");
```

**printk**: Giống printf thôi.

![alt text](/assets/Linux/Kernel/check_hello_kerne;.png)

Sau khi make rồi sẽ có file **.ko* lúc này làm sao để load chương trình vào kernel chạy.

![alt text](/assets/Linux/Kernel/load_in_kernel.png)

kiểm tra sau chạy

![alt text](/assets/Linux/Kernel/check_ko.png)

Bây giờ muốn xóa module đó đi dùng  như sau

![alt text](/assets/Linux/Kernel/remove_module.png)

4. **Module information**
    Sử dụng modinfo đẻ xem thông tin module
    - License: Giấy phép GPL (Cho phép dùng mã nguồn kernel free).
    - Author: Nhà phát triển module.
    - Module description: Mô tả sơ bộ về module.
    - Module version: Phiên bản module.

![alt text](/assets/Linux/Kernel/infor_kernel.png)

## 📊 Standard Format Character Device Driver

### 4.1 🔒 Allocate device number (major/ minor)
   - Application giao tiếp vói phần cứng như thế nào?
   - Device number
      1. Major number: Là số xác định liên kết giữa driver và device. Một major number có thể được chia sẻ giữa nhiều device driver.
      2. Minor number: Là số dg để phân biệt các thiết bị vật lý riêng lẻ.
   - Để cấp phát device number ta có thể sử dụng một trong hai cách:
      1. Static allocating
      2. Dynamic allocating

- Cách đăng kí **device number**:
```c
    /* 1. Allocating device number (cat /pro/devices)*/
    if (alloc_chrdev_region(&mdev.dev_num, 0, 1, "m-cdev") < 0) {
	    pr_err("Failed to alloc chrdev region\n");
	    return -1;
    }
    pr_info("Major = %d Minor = %d\n", MAJOR(mdev.dev_num), MINOR(mdev.dev_num));
```

- Hàm exit:
```c
unregister_chrdev_region(mdev.dev_num, 1);      /* 01 */
```

   - Device file nằm trong thư mục
```bash
ls -l /dev/
```

![alt text](/assets/Linux/Kernel/start.png)

**Ví dụ:** có chương trình blink led ví dụ trên beaglebone. Đầu tiên application đọc ghi vào trong **Device File** (giống chân điều khiển led). Ví dụ **Device File** /dev/ttyS0 là 1 device file đi, thì nó được gán bằng 1 device number thì nó sẽ tham chiếu đến **device number** (major và minor) tương ứng với **hardware** từ 2 số đó sẽ tìm đúng **Device Driver**.

### 4.2 ⚡ Create Device File
Để tạo ra **device file** thì phải tạo **Class device** và **device file**.

Đối với **Class Device** có create và destroy:
```c
    /* 02. Creating struct class */
    if ((mdev.m_class = class_create(THIS_MODULE, "m_class")) == NULL) {
        pr_err("Cannot create the struct class for my device\n");
        goto rm_device_numb;
    }

   rm_device_numb:
    unregister_chrdev_region(mdev.dev_num, 1);
```
Đối với **device file** có create và destroy:

```c
    /* 03. Creating device*/
    if ((device_create(mdev.m_class, NULL, mdev.dev_num, NULL, "m_device")) == NULL) {
        pr_err("Cannot create my device\n");
        goto rm_class;
    }

    rm_class:
    class_destroy(mdev.m_class);
```

Hàm **Exit**:
```c
__exit hello_world_exit(void)   /* Destructor */
{
    device_destroy(mdev.m_class, mdev.dev_num);     /* 03 */
    class_destroy(mdev.m_class);                    /* 02 */
    unregister_chrdev_region(mdev.dev_num, 1);      /* 01 */

    pr_info("Goodbye\n");
}
```

Để kiểm trả **device file**:
```bash
ls -l /dev/ | grep 243
```

### 4.3 🌐 Register File operations
   - Struct **inode** là stuct chứa toàn bộ thông tin của một file.
   - Lưu ý khác biệt giữa open file và file.

Đây là 1 struct **inode** ví dụ:

![alt text](/assets/Linux/Kernel/struct_inode.png)

Đây là ví dụ struct **cdev**:

![alt text](/assets/Linux/Kernel/struct_cdev.png)

Struct **cdev** là một phần tử của struct **inode**. Là struct đại diện cho character **device**.

Struct **file_openrations** là một phần tủ của struct **cdev**. Định nghĩa toàn bộ các hoạt động của file như (open, read, write, close, mmap, ioctl).

![alt text](/assets/Linux/Kernel/struct_file_openration.png)

